TRUNCATE `activity`;
TRUNCATE `activity_log`;
TRUNCATE `apl_userasetlist`;
TRUNCATE `aset`;
TRUNCATE `asetlain`;
TRUNCATE `bangunan`;
TRUNCATE `jaringan`;
TRUNCATE `kapitalisasi`;
TRUNCATE `kdp`;
TRUNCATE `kontrak`;
TRUNCATE `log_aset`;
TRUNCATE `log_asetlain`;
TRUNCATE `log_bangunan`;
TRUNCATE `log_jaringan`;
TRUNCATE `log_kdp`;
TRUNCATE `log_kontrak`;
TRUNCATE `log_merger`;
TRUNCATE `log_mesin`;
TRUNCATE `log_sp2d`;
TRUNCATE `log_sp2d_rinc`;
TRUNCATE `log_tanah`;
TRUNCATE `mesin`;
TRUNCATE `mutasi`;
TRUNCATE `mutasiaset`;
TRUNCATE `pemanfaatan`;
TRUNCATE `pemanfaatanaset`;
TRUNCATE `pemeliharaan`;
TRUNCATE `pemeliharaan_aset`;
TRUNCATE `pemusnahan`;
TRUNCATE `penggunaan`;
TRUNCATE `penggunaanaset`;
TRUNCATE `penghapusan`;
TRUNCATE `penghapusanaset`;
TRUNCATE `penilaian`;
TRUNCATE `penyusutan`;
TRUNCATE `penyusutanaset`;
TRUNCATE `penyusutan_pertahun`;
TRUNCATE `penyusutan_tahun`;
TRUNCATE `penyusutan_tahun_pertama`;
TRUNCATE `perencanaan`;
TRUNCATE `prcn_asettetaplain`;
TRUNCATE `prcn_bangunan`;
TRUNCATE `prcn_jaringan`;
TRUNCATE `prcn_kdp`;
TRUNCATE `prcn_mesin`;
TRUNCATE `prcn_tanah`;
TRUNCATE `regrouping`;
TRUNCATE `rencana`;
TRUNCATE `rencana_pemeliharaan`;
TRUNCATE `sp2d`;
TRUNCATE `sp2d_rinc`;
TRUNCATE `standarharga`;
TRUNCATE `standarkebutuhan`;
TRUNCATE `tanah`;
TRUNCATE `tmp_asetlain`;
TRUNCATE `tmp_merger`;
TRUNCATE `tmp_mesin`;
TRUNCATE `tmp_register`;
TRUNCATE `transfer`;
TRUNCATE `transferaset`;
TRUNCATE `usulan`;
TRUNCATE `usulanaset`;
